<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <corp.katet.evernote.image.FingerPaintView
        android:id="@+id/finger_paint"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:visibility="visible" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/draw_mode_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="90dp"
        android:layout_marginRight="24dp"
        android:onClick="changeDrawMode"
        app:fabSize="mini"
        app:elevation="6dp"
        app:pressedTranslationZ="12dp"
        app:borderWidth="0dp"
        app:backgroundTint="#C3C3C3"
        app:rippleColor="#C3C3C3" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/note_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/ic_save"
        android:onClick="askNoteTitle"
        app:fabSize="normal"
        app:elevation="6dp"
        app:pressedTranslationZ="12dp"
        app:borderWidth="0dp"
        app:backgroundTint="#16A600"
        app:rippleColor="#16A600" />

</android.support.design.widget.CoordinatorLayout>